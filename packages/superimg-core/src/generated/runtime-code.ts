// Auto-generated by scripts/generate-runtime.ts
// DO NOT EDIT - regenerate with: pnpm run generate
//
// This is the bundled runtime code for browser template compilation.
// Templates that import from "superimg" get this code injected.

export const RUNTIME_CODE = "// src/shared/utils.ts\nfunction parseDuration(d, fieldName = \"duration\", fps) {\n  if (typeof d === \"number\") return d;\n  if (d === void 0) return 5;\n  const frameMatch = d.match(/^(\\d+)(?:\\.\\d+)?f$/);\n  if (frameMatch) {\n    if (fps === void 0) {\n      throw new Error(\n        `Invalid ${fieldName}: \"${d}\". Frame duration requires fps to be specified.`\n      );\n    }\n    return parseFloat(frameMatch[1]) / fps;\n  }\n  const match = d.match(/^(\\d+(?:\\.\\d+)?)(ms|s)$/);\n  if (!match) {\n    throw new Error(\n      `Invalid ${fieldName}: \"${d}\". Expected format: \"5s\", \"500ms\", or \"30f\"`\n    );\n  }\n  const value = parseFloat(match[1]);\n  const unit = match[2];\n  return unit === \"ms\" ? value / 1e3 : value;\n}\n\n// ../superimg-stdlib/dist/easing.js\nfunction clamp01(t) {\n  return Math.max(0, Math.min(1, t));\n}\nfunction linear(t) {\n  return clamp01(t);\n}\nfunction easeInQuad(t) {\n  const x = clamp01(t);\n  return x * x;\n}\nfunction easeOutQuad(t) {\n  const x = 1 - clamp01(t);\n  return 1 - x * x;\n}\nfunction easeInOutQuad(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? 2 * x * x : 1 - Math.pow(-2 * x + 2, 2) / 2;\n}\nfunction easeInSine(t) {\n  const x = clamp01(t);\n  return 1 - Math.cos(x * Math.PI / 2);\n}\nfunction easeOutSine(t) {\n  const x = clamp01(t);\n  return Math.sin(x * Math.PI / 2);\n}\nfunction easeInOutSine(t) {\n  const x = clamp01(t);\n  return -(Math.cos(Math.PI * x) - 1) / 2;\n}\nfunction easeInCubic(t) {\n  const x = clamp01(t);\n  return x * x * x;\n}\nfunction easeOutCubic(t) {\n  const x = 1 - clamp01(t);\n  return 1 - x * x * x;\n}\nfunction easeInOutCubic(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? 4 * x * x * x : 1 - Math.pow(-2 * x + 2, 3) / 2;\n}\nfunction easeInQuart(t) {\n  const x = clamp01(t);\n  return x * x * x * x;\n}\nfunction easeOutQuart(t) {\n  const x = 1 - clamp01(t);\n  return 1 - x * x * x * x;\n}\nfunction easeInOutQuart(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? 8 * x * x * x * x : 1 - Math.pow(-2 * x + 2, 4) / 2;\n}\nfunction easeInQuint(t) {\n  const x = clamp01(t);\n  return x * x * x * x * x;\n}\nfunction easeOutQuint(t) {\n  const x = 1 - clamp01(t);\n  return 1 - x * x * x * x * x;\n}\nfunction easeInOutQuint(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? 16 * x * x * x * x * x : 1 - Math.pow(-2 * x + 2, 5) / 2;\n}\nfunction easeInExpo(t) {\n  const x = clamp01(t);\n  return x === 0 ? 0 : Math.pow(2, 10 * (x - 1));\n}\nfunction easeOutExpo(t) {\n  const x = clamp01(t);\n  return x === 1 ? 1 : 1 - Math.pow(2, -10 * x);\n}\nfunction easeInOutExpo(t) {\n  const x = clamp01(t);\n  if (x === 0) return 0;\n  if (x === 1) return 1;\n  return x < 0.5 ? Math.pow(2, 20 * x - 10) / 2 : (2 - Math.pow(2, -20 * x + 10)) / 2;\n}\nfunction easeInCirc(t) {\n  const x = clamp01(t);\n  return 1 - Math.sqrt(1 - x * x);\n}\nfunction easeOutCirc(t) {\n  const x = clamp01(t) - 1;\n  return Math.sqrt(1 - x * x);\n}\nfunction easeInOutCirc(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? (1 - Math.sqrt(1 - Math.pow(2 * x, 2))) / 2 : (Math.sqrt(1 - Math.pow(-2 * x + 2, 2)) + 1) / 2;\n}\nvar BACK_C1 = 1.70158;\nvar BACK_C3 = 2.70158;\nfunction easeInBack(t) {\n  const x = clamp01(t);\n  return BACK_C3 * x * x * x - BACK_C1 * x * x;\n}\nfunction easeOutBack(t) {\n  const x = clamp01(t);\n  return 1 + BACK_C3 * Math.pow(x - 1, 3) + BACK_C1 * Math.pow(x - 1, 2);\n}\nfunction easeInOutBack(t) {\n  const x = clamp01(t);\n  const c2 = BACK_C1 * 1.525;\n  return x < 0.5 ? Math.pow(2 * x, 2) * (3.5949095 * 2 * x - c2) / 2 : (Math.pow(2 * x - 2, 2) * (3.5949095 * (x * 2 - 2) + c2) + 2) / 2;\n}\nvar ELASTIC_C4 = 2 * Math.PI / 3;\nfunction easeInElastic(t) {\n  const x = clamp01(t);\n  if (x === 0) return 0;\n  if (x === 1) return 1;\n  return -Math.pow(2, 10 * x - 10) * Math.sin((x * 10 - 10.75) * ELASTIC_C4);\n}\nfunction easeOutElastic(t) {\n  const x = clamp01(t);\n  if (x === 0) return 0;\n  if (x === 1) return 1;\n  return Math.pow(2, -10 * x) * Math.sin((x * 10 - 0.75) * ELASTIC_C4) + 1;\n}\nfunction easeInOutElastic(t) {\n  const x = clamp01(t);\n  if (x === 0) return 0;\n  if (x === 1) return 1;\n  return x < 0.5 ? -(Math.pow(2, 20 * x - 10) * Math.sin((20 * x - 11.125) * (2 * Math.PI) / 4.5)) / 2 : Math.pow(2, -20 * x + 10) * Math.sin((20 * x - 11.125) * (2 * Math.PI) / 4.5) / 2 + 1;\n}\nfunction bounceOut(t) {\n  const n1 = 7.5625;\n  const d1 = 2.75;\n  if (t < 1 / d1) return n1 * t * t;\n  else if (t < 2 / d1) return n1 * (t -= 1.5 / d1) * t + 0.75;\n  else if (t < 2.5 / d1) return n1 * (t -= 2.25 / d1) * t + 0.9375;\n  else return n1 * (t -= 2.625 / d1) * t + 0.984375;\n}\nfunction easeInBounce(t) {\n  return 1 - bounceOut(1 - clamp01(t));\n}\nfunction easeOutBounce(t) {\n  return bounceOut(clamp01(t));\n}\nfunction easeInOutBounce(t) {\n  const x = clamp01(t);\n  return x < 0.5 ? (1 - bounceOut(1 - 2 * x)) / 2 : (1 + bounceOut(2 * x - 1)) / 2;\n}\n\n// src/composition/transitions.ts\nvar EASING_MAP = {\n  linear,\n  easeInQuad,\n  easeOutQuad,\n  easeInOutQuad,\n  easeInSine,\n  easeOutSine,\n  easeInOutSine,\n  easeInCubic,\n  easeOutCubic,\n  easeInOutCubic,\n  easeInQuart,\n  easeOutQuart,\n  easeInOutQuart,\n  easeInQuint,\n  easeOutQuint,\n  easeInOutQuint,\n  easeInExpo,\n  easeOutExpo,\n  easeInOutExpo,\n  easeInCirc,\n  easeOutCirc,\n  easeInOutCirc,\n  easeInBack,\n  easeOutBack,\n  easeInOutBack,\n  easeInElastic,\n  easeOutElastic,\n  easeInOutElastic,\n  easeInBounce,\n  easeOutBounce,\n  easeInOutBounce\n};\nfunction applyEasing(t, easingName) {\n  if (!easingName) return t;\n  const fn = EASING_MAP[easingName];\n  return fn ? fn(t) : t;\n}\nfunction renderWithTransition(html, transition, progress, phase = \"enter\") {\n  if (transition.type === \"none\") {\n    return html;\n  }\n  const clampedProgress = Math.max(0, Math.min(progress, 1));\n  const linearT = phase === \"exit\" ? 1 - clampedProgress : clampedProgress;\n  const t = applyEasing(linearT, transition.easing);\n  let opacity = 1;\n  let transform = \"none\";\n  switch (transition.type) {\n    case \"fade\":\n      opacity = t;\n      break;\n    case \"slide-left\":\n      transform = phase === \"enter\" ? `translateX(${(1 - t) * 100}%)` : `translateX(${-t * 100}%)`;\n      break;\n    case \"slide-right\":\n      transform = phase === \"enter\" ? `translateX(${(1 - t) * -100}%)` : `translateX(${t * 100}%)`;\n      break;\n    case \"slide-up\":\n      transform = phase === \"enter\" ? `translateY(${(1 - t) * 100}%)` : `translateY(${-t * 100}%)`;\n      break;\n    case \"slide-down\":\n      transform = phase === \"enter\" ? `translateY(${(1 - t) * -100}%)` : `translateY(${t * 100}%)`;\n      break;\n    default:\n      return html;\n  }\n  const styles = [\n    `position:absolute`,\n    `inset:0`,\n    `width:100%`,\n    `height:100%`\n  ];\n  if (opacity !== 1) {\n    styles.push(`opacity:${opacity}`);\n  }\n  if (transform !== \"none\") {\n    styles.push(`transform:${transform}`);\n  }\n  return `<div style=\"${styles.join(\";\")}\">${html}</div>`;\n}\n\n// src/composition/compose.ts\nfunction isSceneDefinition(item) {\n  return \"template\" in item && typeof item.template === \"object\";\n}\nfunction normalizeToSceneDefinitions(items) {\n  return items.map(\n    (item) => isSceneDefinition(item) ? item : { template: item }\n  );\n}\nfunction resolveTransition(t, fps) {\n  if (!t || t.type === \"none\") return void 0;\n  const durationSeconds = parseDuration(\n    t.duration,\n    \"transition.duration\",\n    fps\n  );\n  return {\n    type: t.type,\n    duration: durationSeconds,\n    easing: t.easing\n  };\n}\nfunction compose(scenes) {\n  if (scenes.length === 0) {\n    throw new Error(\"compose() requires at least one scene\");\n  }\n  const definitions = normalizeToSceneDefinitions(scenes);\n  const mergedConfig = mergeConfigs(definitions.map((d) => d.template));\n  const fps = mergedConfig.fps ?? 30;\n  const resolvedScenes = [];\n  let currentFrame = 0;\n  for (let i = 0; i < definitions.length; i++) {\n    const def = definitions[i];\n    const template = def.template;\n    const cfg = template.config;\n    const durationSource = def.duration ?? cfg?.duration;\n    const durationSeconds = parseDuration(\n      durationSource,\n      `scene[${i}].duration`,\n      fps\n    );\n    const totalFrames2 = Math.round(durationSeconds * fps);\n    const id = def.id ?? `scene-${i}`;\n    const enterTransition = resolveTransition(def.enter, fps);\n    const exitTransition = resolveTransition(def.exit, fps);\n    resolvedScenes.push({\n      id,\n      label: def.label,\n      index: i,\n      template,\n      startFrame: currentFrame,\n      endFrame: currentFrame + totalFrames2,\n      totalFrames: totalFrames2,\n      duration: durationSeconds,\n      data: { ...template.sample, ...def.data },\n      enterTransition,\n      exitTransition\n    });\n    currentFrame += totalFrames2;\n  }\n  const totalFrames = currentFrame;\n  const totalDurationSeconds = totalFrames / fps;\n  const frameToSceneIndex = new Uint16Array(totalFrames);\n  for (const s of resolvedScenes) {\n    for (let f = s.startFrame; f < s.endFrame; f++) {\n      frameToSceneIndex[f] = s.index;\n    }\n  }\n  const config = {\n    ...mergedConfig,\n    duration: totalDurationSeconds\n  };\n  const result = {\n    kind: \"video\",\n    type: \"composed\",\n    scenes: resolvedScenes,\n    totalFrames,\n    duration: totalDurationSeconds,\n    fps,\n    config,\n    getScene(index) {\n      return resolvedScenes[index];\n    },\n    getSceneById(id) {\n      return resolvedScenes.find((s) => s.id === id);\n    },\n    getSceneAtFrame(frame) {\n      const clamped = Math.max(0, Math.min(Math.floor(frame), totalFrames - 1));\n      const idx = frameToSceneIndex[clamped];\n      return resolvedScenes[idx];\n    },\n    render(ctx) {\n      const frame = Math.min(ctx.globalFrame, totalFrames - 1);\n      const scene2 = result.getSceneAtFrame(frame);\n      const sceneFrame = frame - scene2.startFrame;\n      const sceneProgress = scene2.totalFrames > 1 ? Math.min(sceneFrame / (scene2.totalFrames - 1), 1) : 1;\n      const sceneTimeSeconds = sceneFrame / ctx.fps;\n      const sceneCtx = {\n        ...ctx,\n        sceneIndex: scene2.index,\n        sceneId: scene2.id,\n        sceneFrame,\n        sceneTimeSeconds,\n        sceneProgress,\n        sceneTotalFrames: scene2.totalFrames,\n        sceneDurationSeconds: scene2.duration,\n        // scene.data already has: defaults + def.data\n        // Only merge runtime ctx.data on top\n        data: { ...scene2.data, ...ctx.data }\n      };\n      let html = scene2.template.render(sceneCtx);\n      if (scene2.enterTransition && scene2.enterTransition.duration > 0) {\n        const enterFrames = Math.round(\n          scene2.enterTransition.duration * fps\n        );\n        if (sceneFrame < enterFrames) {\n          const progress = sceneFrame / enterFrames;\n          html = renderWithTransition(html, scene2.enterTransition, progress);\n        }\n      }\n      if (scene2.exitTransition && scene2.exitTransition.duration > 0) {\n        const exitFrames = Math.round(\n          scene2.exitTransition.duration * fps\n        );\n        const exitStartFrame = scene2.totalFrames - exitFrames;\n        if (sceneFrame >= exitStartFrame) {\n          const progress = (sceneFrame - exitStartFrame) / exitFrames;\n          html = renderWithTransition(\n            html,\n            scene2.exitTransition,\n            progress,\n            \"exit\"\n          );\n        }\n      }\n      return html;\n    },\n    getCheckpoints() {\n      return resolvedScenes.map((s) => ({\n        id: s.id,\n        frame: s.startFrame,\n        time: s.startFrame / fps,\n        label: s.label,\n        metasample: { sceneIndex: s.index },\n        source: { type: \"scene\", sceneId: s.id }\n      }));\n    }\n  };\n  return result;\n}\nfunction mergeConfigs(scenes) {\n  const fonts = /* @__PURE__ */ new Set();\n  const stylesheets = /* @__PURE__ */ new Set();\n  const inlineCss = [];\n  let width;\n  let height;\n  let fps;\n  let widthSource;\n  let heightSource;\n  let fpsSource;\n  const warnings = [];\n  for (let i = 0; i < scenes.length; i++) {\n    const cfg = scenes[i].config;\n    if (!cfg) continue;\n    cfg.fonts?.forEach((f) => fonts.add(f));\n    cfg.stylesheets?.forEach((s) => stylesheets.add(s));\n    cfg.inlineCss?.forEach((c) => inlineCss.push(c));\n    if (cfg.width !== void 0) {\n      if (width === void 0) {\n        width = cfg.width;\n        widthSource = i;\n      } else if (cfg.width !== width) {\n        warnings.push(\n          `Width conflict: scene[${widthSource}] defines ${width}px, scene[${i}] defines ${cfg.width}px. Using ${width}px.`\n        );\n      }\n    }\n    if (cfg.height !== void 0) {\n      if (height === void 0) {\n        height = cfg.height;\n        heightSource = i;\n      } else if (cfg.height !== height) {\n        warnings.push(\n          `Height conflict: scene[${heightSource}] defines ${height}px, scene[${i}] defines ${cfg.height}px. Using ${height}px.`\n        );\n      }\n    }\n    if (cfg.fps !== void 0) {\n      if (fps === void 0) {\n        fps = cfg.fps;\n        fpsSource = i;\n      } else if (cfg.fps !== fps) {\n        warnings.push(\n          `FPS conflict: scene[${fpsSource}] defines ${fps}fps, scene[${i}] defines ${cfg.fps}fps. Using ${fps}fps.`\n        );\n      }\n    }\n  }\n  for (const w of warnings) {\n    console.warn(`[compose] ${w}`);\n  }\n  return {\n    width,\n    height,\n    fps,\n    fonts: [...fonts],\n    stylesheets: [...stylesheets],\n    inlineCss\n  };\n}\n\n// src/composition/scene.ts\nfunction scene(template, options) {\n  return { template, ...options };\n}\n\n// <stdin>\nfunction defineScene(m) {\n  return { ...m, kind: \"video\" };\n}\nfunction defineImage(m) {\n  return { ...m, kind: \"image\" };\n}\nfunction defineGif(m) {\n  return { ...m, kind: \"gif\" };\n}\nfunction defineSvg(m) {\n  return { ...m, kind: \"svg\" };\n}\nfunction defineConfig(c) {\n  return c;\n}\nexport {\n  compose,\n  defineConfig,\n  defineGif,\n  defineImage,\n  defineScene,\n  defineSvg,\n  scene\n};\n//! Utility functions for SuperImg core\n//! Transition rendering - wrap scene HTML with CSS transitions\n//! Scene composition - combine multiple templates into a single video\n//! Scene helper for compose() - wraps a template with scene options\n";
