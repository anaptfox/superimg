# Ollama runs locally — no API key needed.
# Override the base URL if Ollama is on a remote host or non-default port:
# OLLAMA_BASE_URL=http://localhost:11434

# Model to use. Defaults to llama3.2.
# Any model that supports tool calling works, e.g. llama3, mistral, qwen2.5
# OLLAMA_MODEL=llama3.2
