{
  "name": "superimg-example-assets-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Asset API demo with backgrounds, audio, and template defaults",
  "type": "module",
  "scripts": {
    "setup": "superimg setup",
    "dev": "superimg dev ./templates/intro.video.ts",
    "render": "superimg render --all",
    "clean": "rm -rf output"
  },
  "dependencies": {
    "superimg": "workspace:*"
  }
}
