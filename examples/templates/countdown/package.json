{
  "name": "superimg-example-countdown",
  "version": "0.1.0",
  "private": true,
  "description": "Countdown timer example for SuperImg",
  "type": "module",
  "scripts": {
    "dev": "pnpm exec superimg dev template",
    "render": "pnpm exec superimg render template -o output.mp4",
    "clean": "true"
  },
  "dependencies": {
    "superimg": "workspace:*"
  }
}